# Copy to .env and fill in. The .env file is gitignored and must never be committed.
# Used only by the judge stage (scoring responses); generation is fully local.
ANTHROPIC_API_KEY=sk-ant-your-key-here
# Phase 2 behavior-axis judge (Gemini). Same rule: local judging only.
GEMINI_API_KEY=your-gemini-key-here
