[project]
name = "idt-organism"
version = "0.1.0"
description = "Phase 0 model organism for Instrumental Differential Treatment (IDT): a prompted organism, a matched control, and a preregistered divergence test."
requires-python = ">=3.12"
dependencies = [
    "tqdm>=4.66",
    "numpy>=1.26",
    "anthropic>=0.40",
    "google-genai>=1.0",
    "pydantic>=2.0",
    "python-dotenv>=1.0",
]

[project.optional-dependencies]
# Generation runs locally; the analysis stack alone doesn't need torch.
local = [
    "torch>=2.4",
    "transformers>=4.45",
    "accelerate>=1.0",
]
# Phase 3: LoRA distillation of the objective into weights. Adds exactly one
# package to [local] -- the training loop is hand-rolled (script/train_lora_organism.py)
# because Qwen2.5's chat template lacks the markers a trainer framework needs to
# mask the prompt correctly.
train = [
    "peft>=0.13",
]
dev = [
    "pytest>=8.0",
    "ruff>=0.6",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"]

[tool.ruff]
line-length = 100
