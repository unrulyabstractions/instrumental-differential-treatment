# Secrets — never committed. Real key lives in .env (see .env.example).
.env

# Experiment artifacts: response corpus and derived data stay local (dual-use policy).
out/

# Notes on an under-review (anonymized) external paper — never published.
docs/paper_notes/

# Claude Code: machine-local settings stay local; shared skills ARE committed.
.claude/settings.local.json

# Python
__pycache__/
*.pyc
.venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# OS / editor
.DS_Store
