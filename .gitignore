tmp/
out/
models/
/data/
jobs/
paper/main.pdf
paper/build/
*.aux
*.bbl
*.blg
*.fdb_latexmk
*.fls
*.log
*.out
*.synctex.gz
.DS_Store
.backups/
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/
.env
*.egg-info/

# Session backups of r1 output and a retired code copy. Redundant with the
# published HuggingFace dataset and out/; this project keeps no legacy directory.
archive/

# A 41 MB submission bundle, rebuildable from paper/. Kept out of history.
paper.zip

# Claude Code session state
.claude/

# Agent knowledge base: orientation notes for AI sessions. Never bundled.
agents/
